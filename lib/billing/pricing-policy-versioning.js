export function nextPolicyVersion(previousVersion = '2026-09-06-v1') { const match = previousVersion.match(/^(\d{4}-\d{2}-\d{2})-v(\d+)$/); return match ? `${new Date().toISOString().slice(0, 10)}-v${Number(match[2]) + 1}` : `${new Date().toISOString().slice(0, 10)}-v1`; }
export function createPolicyDraft(policy, simulation) { return { policy, simulation, status: 'draft', createdAt: new Date().toISOString(), requiresAdminConfirmation: true }; }
