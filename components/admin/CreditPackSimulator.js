import { CREDIT_PACKS } from '@/lib/billing/package-economics';
export default function CreditPackSimulator() { return <section className="cinex-economics-section"><h2>Credit-pack simulator</h2><p>Pack definitions are available for simulation, but no profitability result is shown until verified provider costs and payment-fee data are configured.</p><table><thead><tr><th>Pack</th><th>Customer price</th><th>Credits</th><th>Bonus</th><th>Verified scenario result</th></tr></thead><tbody>{CREDIT_PACKS.map((pack) => <tr key={pack.id}><td>{pack.name}</td><td>${(pack.priceCents / 100).toFixed(2)}</td><td>{pack.creditsGranted}</td><td>{pack.bonusCredits}</td><td>Unavailable — verified cost source required</td></tr>)}</tbody></table></section>; }
