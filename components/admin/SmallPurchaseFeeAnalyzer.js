import { smallPurchaseAnalysis } from '@/lib/billing/payment-fee-model';
export default function SmallPurchaseFeeAnalyzer() { const rows = smallPurchaseAnalysis(); return <section className="cinex-economics-section"><h2>Small-purchase fixed-fee drag</h2><p>Illustrative fee arithmetic is not account-verified and is not used as actual finance data. Configure account/country fees before publication.</p><table><thead><tr><th>Purchase</th><th>Configured fee</th><th>Effective rate</th></tr></thead><tbody>{rows.map((row) => <tr key={row.grossCents}><td>${(row.grossCents / 100).toFixed(2)}</td><td>Not verified</td><td>Not verified</td></tr>)}</tbody></table></section>; }
